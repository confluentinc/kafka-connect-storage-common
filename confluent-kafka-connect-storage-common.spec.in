Summary: Kafka Connect connectors for copying data between Kafka and Cloud Storage.
Name: confluent-kafka-connect-storage-common
Version: ##RPMVERSION##
Release: ##RPMRELEASE##
Group: Applications/Internet
License: Confluent Community License
Source0: confluent-kafka-connect-storage-common-%{version}.tar.gz
URL: http://confluent.io
BuildRoot: %{_tmppath}/%{name}-%{version}-root
Vendor: Confluent, Inc.
Packager: Confluent Packaging <packages@confluent.io>
BuildArch: noarch

Requires: confluent-common

%description

Kafka Connect Storage Common contains packages used by connectors copying data between Kafka and distributed or cloud storage.
Examples of such connectors are HDFS connector, S3 connector and others.

%define __jar_repack %{nil}

%pre

exit 0

%post

%preun

%postun

%prep

%setup

%build

%install

# Clean out any previous builds not on slash
[ "%{buildroot}" != "/" ] && %{__rm} -rf %{buildroot}

%{__mkdir_p} %{buildroot}
%{__cp} -R * %{buildroot}
# The spec file gets included, get rid of it
%{__rm} %{buildroot}/confluent-kafka-connect-storage-common.spec

%files
%defattr(-,root,root)
/etc/kafka-connect-storage-common
/usr/share/java/kafka-connect-storage-common
/usr/share/doc/kafka-connect-storage-common

%clean
#used to cleanup things outside the build area and possibly inside.
[ "%{buildroot}" != "/" ] && %{__rm} -rf %{buildroot}

%changelog
* Thu Jan 26 2017 Confluent Packaging <packages@confluent.io>
- Initial import
